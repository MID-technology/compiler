class Main is
  this() is
    var i : Integer(7)
    var r : Real(2.5)
    var b : Boolean(true)
    IO().WriteLine(i.ToString())
    IO().WriteLine(r.ToString())
    IO().WriteLine(b.ToString())
  end
end
